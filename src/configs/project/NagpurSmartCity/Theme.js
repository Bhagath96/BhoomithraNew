import { Blue } from '../../../assets/theme';

const themes = {
    local: {
        ...Blue
    },
    dev: {
        ...Blue
    },
    stage: {
        ...Blue
    },
    test: {
        ...Blue
    },
    prod: {
        ...Blue
    }
};

export default themes;
